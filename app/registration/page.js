import Register from "@/utils/components/Register/Register"; // Importing the Register component

/**
 * RegistrationPage component serves as the entry point for user registration.
 *
 * @returns {JSX.Element} The rendered RegistrationPage component
 */
function RegistrationPage() {
  return <Register />; // Render the Register component
}

export default RegistrationPage; // Export the RegistrationPage component as the default export
